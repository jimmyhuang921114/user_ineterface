# 🏥 醫院藥物管理系統 - Python 依賴

# 核心 Web 框架
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# 數據驗證和序列化
pydantic>=2.0.0

# YAML處理
PyYAML>=6.0

# HTTP請求 (用於ROS2節點)
requests>=2.28.0

# CORS 中間件 (已包含在 FastAPI 中)
# fastapi 已包含 starlette，所以 CORS 功能已經可用

# 可選依賴 (用於擴展功能)
# python-multipart>=0.0.6  # 用於表單數據處理
# python-jose[cryptography]>=3.3.0  # 用於 JWT 認證
# passlib[bcrypt]>=1.7.4  # 用於密碼雜湊
# sqlalchemy>=2.0.0  # 用於資料庫操作
# alembic>=1.12.0  # 用於資料庫遷移

# 開發和測試依賴
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
# httpx>=0.24.0  # 用於測試 API 調用

# 生產環境建議
# gunicorn>=21.0.0  # 用於生產環境部署